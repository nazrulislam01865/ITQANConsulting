<section class="hero">
  <div class="container">
    <div class="label reveal">{{ $hero['label'] }}</div>
    <h1 class="reveal">{{ $hero['title'] }}</h1>
    <p class="reveal">{{ $hero['description'] }}</p>
  </div>
</section>
